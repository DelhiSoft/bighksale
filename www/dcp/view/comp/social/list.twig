<section
	class="col-lg-12 connectedSortable">
	<!-- Custom tabs (Charts with tabs)-->
	<div class="card">
		<div class="card-header">
			<h3 class="card-title">
				<i class="fas fa-chart-pie mr-1"></i>
				Contact Info
			</h3>

		</div>

		<div class="card-body">
			<form method="post">
				<table class="table">
					<tr>
						<th>Name</th>
						<th>Link</th>
					</tr>
					{% for info in data %}
						<tr>
							<td>{{info.name}}</td>
							<td><input class='form-control' type="text" name="{{info.name}}" value="{{info.link}}"></td>
						</tr>
					{% endfor %}
					<tr>
						<td colspan="3">
							<input type="hidden" name="action" value="update-socials">
							<button class="form-control">Submit</button>
						</td>
					</tr>
				</table>
			</form>
		</div>
	</div>
</section>
